export interface Employee {
  _id?: string;
  email: string;
  firstName: string;
  lastName: string;
  userName: string;
}

export interface EmployeeIds {
  departmentId: string;
  employeeId: string;
}
